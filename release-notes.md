# v1.2.3 更新内容

## 新增
- **缓存过期开关** — 配置面板新增「缓存过期」选项，默认关闭（缓存永不过期）。开启后缓存 90 天过期，后台自动提前刷新即将过期的数据
- **自适应缓存刷新** — 开启过期后，后台根据积压量自动调节刷新频率，空闲时段提前刷新 7 天内将过期的条目

## 优化
- 缓存 TTL 从 7 天延长至 90 天（需开启缓存过期才生效）
- 3D 倾斜效果现在对详情弹窗生效
